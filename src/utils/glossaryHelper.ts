import type { GlossaryTerm } from '../types';

/**
 * Process text and wrap glossary terms with tooltips
 * This is a placeholder for future enhancement if needed
 */
export const enhanceTextWithGlossary = (
  text: string,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _glossary: GlossaryTerm[]
): string => {
  // For now, return the text as-is
  // This can be enhanced in the future to automatically highlight glossary terms
  return text;
};
